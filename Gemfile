source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in render_cow.gemspec.
gemspec

group :development do
  gem 'sqlite3'
end
gem 'puma', '~> 5.2'
# To use a debugger
# gem 'byebug', group: [:development, :test]
